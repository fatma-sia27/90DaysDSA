1. Given an integer array Arr[] of size N. The task is to find sum of it.

class Solution {
    int sum(int arr[], int n) {
        // code here
        
        int sum=0;
        for(int i=0; i<n; i++){
            sum+=arr[i];
        }
        return sum;
    }
}

2. Given an sorted array A of size N. Find number of elements which are less than or equal to given element X.

class Compute {
    
    public long countOfElements(long arr[], long n, long x)
    {
        int count=0;
        for(int i=0; i<n; i++){
            if(arr[i]<=x){
                count++;
            }
        }
        return count;
    }
}

3. Given an array Arr of size N, print all its elements.

class Solution {
    //Just print the space seperated array elements
    void printArray(int arr[], int n) {
        //   code here
        for(int i=0; i<n; i++){
            System.out.print(arr[i]+" ");
        }
    }
}
